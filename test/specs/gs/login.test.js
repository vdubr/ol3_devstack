goog.require('gs.Login');

casper.test.begin('login user', 1, function(test) {
  casper.start().then(function() {

    //    var apiUrl = '???';
    //    var gpId = 11372;
    //    var login = new gs.Login(apiUrl, gpId);

    //     login.login('u@geosense.cz', 'geoportal1')

    //listen events
    test.skip(1);
  }).run(function() {
    test.done();
  });
});
