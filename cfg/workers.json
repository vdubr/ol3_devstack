{
  
   // we need a unique ID
   "id": "%(ID)s_worker",

   "inherits": "base.json",

  /**
   * Files to compile.
   * Path to app.js.
   * Inputs must include path to .../ol3/.../types.js ??
   */
  "inputs": [
    "../../src/gs/worker/%(ID)s.js"
  ],
  "checks": {
    "checkVars": "OFF"
  },
  "mode": "ADVANCED"

}

